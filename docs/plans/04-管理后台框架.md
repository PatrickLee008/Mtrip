# 模块04:管理后台框架(admin-web 基础层)

状态:已完成 | 进度:100% | 依赖:模块02(登录/菜单接口,可先用 mock) | 更新时间:2026-07-28

## 目标

搭建 Vue3 + Vite + TS + Ant Design Vue 管理后台工程底座,落地 UI 设计方案的全局规范(布局、色彩、主题、权限驱动渲染、国际化)。

## 任务清单

### 工程脚手架
- [x] Vite + Vue3 + TS 初始化,依赖:ant-design-vue 4、pinia、vue-router 4、axios、echarts、vue-i18n、dayjs
- [x] 目录规范:src/{api,assets,components,layouts,locales,router,stores,styles,utils,views}
- [x] Axios 封装:统一 baseURL、JWT 头、错误码拦截(40101/40102 跳登录,40301 提示无权限)、统一响应解包

### 全局视觉规范(对应 UI 方案第二章)
- [x] 主题 token 配置:主蓝 #1677FF、辅助橙 #FF7D00、状态色(成功 #00B42A/警告 #FF7D00/危险 #F53F3F/信息 #86909C)
- [x] 中性灰度、财务金额分级色 CSS 变量;圆角 4/8/12px、间距 8px 基准
- [x] 亮色/暗色双主题切换(ConfigProvider algorithm)
- [x] 通用组件:金额显示 AmountText(千分位+分级色)、脱敏显示 MaskedText、状态标签 StatusTag、站点树形选择器 SiteTreeSelect

### 布局(对应 UI 方案第三章)
- [x] 顶部 Header 48px:Logo、全局搜索框、站点切换下拉、待办铃铛、主题切换、语言切换、个人账号区
- [x] 侧边栏 256px 可折叠 64px:手风琴菜单、当前高亮、待办角标、底部版本/站点/身份栏
- [x] PageContainer:面包屑 + 页面标题 + 操作按钮区

### 登录与权限体系
- [x] 登录页(账号密码 + 记住我),JWT 存储与刷新
- [x] 动态路由:后端下发菜单树 → 生成路由与侧边菜单;未匹配 404/403 页
- [x] 按钮权限指令 v-perm(无权限从 DOM 移除)
- [x] 路由守卫:登录态、权限校验、页面标题

### 国际化
- [x] vue-i18n 中/英文语言包骨架,antd locale 联动,货币符号/时区按站点配置渲染

## 验收标准

- npm run dev 可运行,登录后动态菜单渲染,主题/语言切换正常
- npm run build 无 TS 报错

## 完成记录

- 2026-07-28:admin-web 工程底座完成,共 41 个文件。技术栈 Vite 5 + Vue 3.5 + TS + Ant Design Vue 4.2 + Pinia + vue-router 4 + vue-i18n 9。
  - 脚手架:package.json / vite.config.ts(@别名、/api 代理→127.0.0.1:9501、antd/echarts 分包)/ tsconfig / .env.development|production
  - 契约对齐:登录 `POST /api/v1/admin/auth/login`(驼峰 profile)、菜单 `GET /auth/menus`(snake_case 菜单树 + perms)、站点树 `GET /site/tree`;component 字段映射 `src/views/{component}.vue`,解析失败回退 wip 占位页
  - 布局:BasicLayout(Header 48px + Sider 256/64px 折叠 + 底部版本身份栏)、手风琴侧边菜单(图标按名动态渲染)、PageContainer 面包屑
  - 权限:路由守卫刷新恢复(apiMe + loadMenus + addRoute 后 replace 重进)、v-perm 指令(无权限移除 DOM,超管恒真)
  - 主题/国际化:亮暗双主题(ConfigProvider algorithm + CSS 变量)、zh-CN/en-US 语言包、antd locale 与 dayjs 联动
  - 验收:`npm install` 138 packages;`npm run build`(vue-tsc --noEmit + vite build)零 TS 报错,3281 模块打包成功
  - 备注:登录后动态菜单联调需后端 Docker 启动,归入模块08;echarts 空 chunk 提示为正常(数据大屏在模块07 引用)
