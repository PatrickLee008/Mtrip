# 2026-09-15 本地 dev 全量提交记录

## 授权与范围

用户授权提交本地全部代码修改，并明确由用户自行推送远端。本次仅创建本地提交，不执行 push、部署或数据库迁移。提交基线为远端 dev 的 `29614ea`。

## 变更内容

- 商户入驻流程：注册草稿、注册审批、物业 KYC、最终批准、凭证投递、账号激活、登录与恢复；包含后台协助录入、开发测试模式和激活网关修复。
- 酒店物业模型：将酒店经营、房型、库存、订单、财务、营销、排名和消费者收藏等链路迁移至物业与房型标识，保留门票商品模型。
- 前端：商户物业列表和新增页面、导航与账号页面，管理端相关审核页面，以及消费者端物业接口适配。
- 数据库与交付：基础 SQL、增量迁移、权限菜单、回归脚本、接口契约和阶段验收文档。
- dev 整合：保留远端余额支付和关怀模式；My Pick 共享取数接入物业收藏，两种模式的酒店详情参数统一为 propertyId；交接文档保留双方记录。

## 检查记录

- 本次提交前已跟踪文件 `git diff --check` 通过，`git ls-files -u` 为空。全部新增文件暂存后，默认差异检查提示 15 处 Markdown 行尾双空格（硬换行），保留原始排版；关闭行尾空格告警后再次检查其余差异。
- 上一轮合并完成后 client-app TypeScript 检查通过，订单控制器 Docker PHP 语法检查通过。
- 上一轮备份比对：141 个仅本地修改的已跟踪文件及 80 个原未跟踪文件内容均完整保留。
- 各阶段已有测试结果保留在本目录对应报告；本次为代码归档提交，未重新执行完整业务回归、真实支付或部署验收。

## 恢复与后续

- 原始本地改动的 stash 备份名称：`codex-backup-before-dev-sync-2026-09-15`，继续保留。
- 本次提交的完整文件列表与提交 ID 以 Git 日志为准，可通过 `git show --stat HEAD` 查看。
- 用户确认后自行执行 `git push Mtrip dev`。
- 当前 Git 钩子提示 PATH 缺少 git-lfs；上一轮分支同步已完成，新增图片未发现 LFS 指针。推送如受 pre-push 钩子影响，需先恢复 Git LFS 环境。
