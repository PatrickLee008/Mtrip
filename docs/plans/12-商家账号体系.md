# 12 - 商家账号体系改造

> 在集团/门店模块之上,统一商家侧(集团/商户/门店)账号到单表 `merchant_admin`,以显式类型字段区分,门店补齐账号生成。
> 决策依据见记忆《多角色账号统一存储与供应商隔离原则》《商户体系三层架构演进方案》。

## 设计结论

| 主体 | 前端 | 账号表 | 类型标记 | 数据范围 |
| --- | --- | --- | --- | --- |
| 平台 | admin-web | `sys_admin` | 平台 RBAC(sys_role/sys_menu) | 站点 |
| 集团/商户/门店 | merchant-web(未开发) | `merchant_admin` | `account_type` 1集团 2商户 3门店 | 由 group_id/merchant_id/store_id 裁剪 |
| 供应商 | supplier-web(占位) | 未来 `supplier_admin` | 独立 | 独立 |

核心规则:
- **单表 + 显式类型**:`merchant_admin` 加 `account_type`(1集团 2商户 3门店)与 `store_id`(门店账号绑定,0=非门店),不再靠 `merchant_id=0 && group_id>0` 隐式推断集团账号。
- **登录名前缀**:集团 `g` / 商户 `m` / 门店 `s` + 六位补零 ID,冲突追加随机后缀,`uk_username` 全局唯一。
- **权限**:三类共用 `role_perms` JSON 权限机制;商家域菜单/权限键为独立一套(不复用平台 sys_menu),本期仅落地平台侧管理与账号生成,merchant-web 登录侧数据范围裁剪归二期。
- **供应商**:业务域/生命周期不同,账号与权限完全独立,不并入 merchant_admin。

## 一期任务清单(本期实施)

### 数据库(database/)
- [x] `merchant/01-merchant.sql`:merchant_admin 增加 `account_type` / `store_id` 列(全新安装)
- [x] `merchant/04-admin-account-type.sql`:存量库幂等加列 + 回填(集团账号→1,其余→2)

### 后端(backend/services/merchant-service)
- [x] `GroupService::resetAccount`:集团账号写 `account_type=1`
- [x] `MerchantService::approve`:商户主账号写 `account_type=2`
- [x] `StoreService`(新增):门店账号生成/重置(s 前缀,`account_type=3` + `store_id`)
- [x] `StoreController::detail`:返回门店账号列表;新增 `accountReset` 动作
- [x] `config/routes.php`:注册 `merchant/store/account-reset`
- [x] `MerchantController::detail`:admins 查询回带 `account_type`

### 前端(admin-web)
- [x] `api/merchant.ts`:`apiStoreAccountReset`;store 详情类型补充
- [x] `views/merchant/store/index.vue`:详情抽屉增门店账号区(生成/重置,明文密码仅弹一次)
- [x] `views/merchant/list/index.vue`:主账号列表增"账号类型"列
- [x] `locales/zh-CN.ts`、`en-US.ts`:`merchant.storePage.storeAccounts` 等词条 + `merchant.accountType.*`
- [x] `database/seed/02-menu.sql`:40605 门店账号按钮权限(`merchant:store:account`)

### 展位目录
- [x] `supplier-web/README.md`:供应商后台独立展位(账号/权限/业务域三独立)
- [x] `merchant-web/README.md`:修正为集团/商户/门店三类账号共用,按 account_type 裁剪

## 二期任务清单(本轮实施,详见 13-商家端merchant-web落地.md)
- [x] merchant-web 登录:按 `account_type` 决定菜单集合与数据范围(集团=全部绑定商户,门店=本门店履约子集)
- [x] 商家域菜单/权限键常量表(独立于平台 sys_menu)→ 落地为 `merchant_menu` 表 + `database/seed/04-merchant-menu.sql`
- [ ] `supplier_admin` 建表 + supplier-web 脚手架(仍未实施)
- [x] `role_perms` JSON → 商户自定义角色四表 RBAC(`merchant_role`/`merchant_role_menu`/`merchant_admin_role`);`role_perms` 列保留不再写入

## 三期任务清单(本轮实施:模块授权 + 角色预设 + 子账号配额)

> 背景:二期落地了四表 RBAC,但平台侧缺三块管控能力 —— 管理端无法授予业务模块、
> 内置角色只有各 account_type 一个"管理员"、子账号数量不设上限。本轮补齐。

### 数据库(database/)
- [x] `seed/06-merchant-role-preset.sql`:补 `商户运营人员`(merchant_ops)/`商户客服`(merchant_cs)
      两个 `is_builtin=1` 预设角色 + 各自菜单子集。**按 `role_code` 判存在插入,不硬编码自增 ID**
      (存量库 ID 4/5 可能已被商户自建角色占用)。
- [x] `merchant/34-merchant-sub-account-limit.sql`:`merchant_info` / `merchant_group`
      各加 `sub_account_limit INT NOT NULL DEFAULT 3`(不含主账号)。
- [x] `merchant/35-merchant-module-grant.sql`:新建 `merchant_module_grant`(商户→模块),
      `merchant_menu` 加 `module_key`(''=公共菜单),回填 600/700 及其按钮为 `hotel`。
- [x] `seed/02-menu.sql`:新增按钮 30115 `merchant:list:module`(功能模块授权)。
- [x] 三个脚本均已登记进 `deploy/docker-compose.yml` 的 initdb 挂载(27a / 39o / 39p)。

### 后端(backend/services/merchant-service)
- [x] `app/Support/MerchantModule.php`(新增):模块目录 `CATALOG`、`sanitize` 白名单过滤、
      `granted`、`visibleModules`(集团账号与"无授权行"均返回 null=不裁剪)。
- [x] `MerchantAuthService`:新增静态 `applyModuleScope()`;`menus()` / `allPermsForType()` /
      `collectPermissions()` 三处统一按模块裁剪 —— **权限快照写在 JWT 里,只过滤菜单不过滤
      perms 会留下越权接口**。
- [x] `Merchant/RoleController`:`menuTree()` / `assign()` 同样施加模块过滤,
      防止商户把未开通模块的菜单授给子账号。
- [x] `Merchant/AccountController`:新增 `quota()`;`create()` 前置配额校验
      (上限取所属主体,已用只数 `is_owner=0`,主体行缺失回落默认 3)。
- [x] `MerchantController`:新增 `modules()` / `moduleGrant()`(覆盖式保存,写后
      `auth_version + 1` 踢商户下线以刷新 JWT 权限快照);`collectFields()` 收
      `subAccountLimit`(0-50);`detail()` 回带 `sub_account_limit` 与 `modules`。
- [x] `config/routes.php`:`admin/merchant/modules`(GET)、`admin/merchant/module-grant`(POST)、
      `merchant/account/quota`(GET)。网关无需改动(`merchant`/`account` 二级模块已在 map 中)。

### 前端
- [x] `admin-web/views/merchant/account/index.vue`:`onMounted` 空关键词预载商户下拉
      (原先只在 `@search` 触发,不打字点开恒为空);未选商户时的提示补 `pleaseSelect` 前缀。
- [x] `admin-web/api/merchant.ts`:`apiMerchantModules` / `apiMerchantModuleGrant`。
- [x] `admin-web/views/merchant/list/index.vue`:详情抽屉增"功能模块授权"区与弹窗
      (`v-perm="'merchant:list:module'"`);编辑表单增"子账号上限"。
- [x] `merchant-web/api/account.ts`:`apiAccountQuota`;`views/account/index.vue` 展示
      `已用/上限` 并在用满时禁用新增按钮。
- [x] 四份 locales(admin-web zh/en、merchant-web zh/en)同步词条。

### 遗留(未做,需另行决策)
- [ ] `restaurant` 模块目前没有专属菜单 —— 商品管理(500)故意保持公共(酒店增值商品与餐饮菜品共用),
      待餐饮专属页面落地后再在 35 号脚本处补 `module_key='restaurant'`。
- [ ] 运营/客服预设只覆盖 `account_type=2`(商户);集团(1)与门店(3)仍只有各自"管理员"一个预设。
- [ ] `MerchantService::approve` 尚未按入驻申请的 `business_type` 自动写入模块授权,
      新商户仍是"无授权行=全开通",需管理员手动授一次。

## 升级说明
- 全新环境:`database/merchant/01→02→03→04` 后执行 `database/seed/02-menu.sql`(INSERT IGNORE 幂等)。
- 存量环境:执行 `database/merchant/04-admin-account-type.sql`(information_schema 探测幂等加列+回填)与 `database/seed/02-menu.sql`。
- 三期存量环境增量(顺序不可颠倒,35 号脚本要回填 04 号种子插入的菜单行):
  ```
  scripts/db-apply.ps1 database/merchant/34-merchant-sub-account-limit.sql `
                       database/merchant/35-merchant-module-grant.sql `
                       database/seed/02-menu.sql `
                       database/seed/06-merchant-role-preset.sql
  ```
  若 `merchant_menu` / `merchant_role` 为空(库建于二期脚本落地之前),需先补
  `database/merchant/05-merchant-rbac.sql` 与 `database/seed/04-merchant-menu.sql`,
  否则商家端"组织与权限"菜单不会出现。

