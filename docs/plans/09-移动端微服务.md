# 模块09:移动端微服务(C端 /api/v1/app/* 接口)

状态:已完成 | 进度:100%(Docker联调归模块08) | 依赖:模块01、模块03 | 更新时间:2026-07-28

## 目标

按技术方案V1微服务拆分,为 C端 Expo 客户端提供 `/api/v1/app/*` 接口。
与管理端(模块06)的关系:**同一业务微服务同时承载双前缀路由**,业务上通过路由前缀 + 鉴权中间件区分,数据模型完全复用。

## 服务归属与公用说明(核心结论)

| 服务 | 管理端路由 | 移动端路由 | 归属说明 |
|------|-----------|-----------|---------|
| system-service(已有) | /api/v1/admin/sys/* 等 | /api/v1/app/site/*(公开站点) | 管理端专属,仅补充 C端公开站点列表/配置接口 |
| user-service(新建) | /api/v1/admin/user/*(模块06补) | /api/v1/app/auth/*、/api/v1/app/user/* | 移动端为主:C端注册/登录/JWT/个人中心;管理端用户管理后续同服务补充 |
| goods-service(公用) | /api/v1/admin/goods/*(模块06) | /api/v1/app/goods/* | **公用**:管理端商品 CRUD/审核,移动端浏览/搜索/详情/价格日历,同库同模型 |
| order-service(公用) | /api/v1/admin/order/*(模块06) | /api/v1/app/order/* | **公用**:管理端订单管理/退款审核,移动端下单/取消/退款申请/核销码 |
| payment-service(公用) | 对账/渠道管理(模块06) | /api/v1/app/pay/* | **公用**:本期在 order-service 内提供 mock 支付,正式 Stripe/PayPal 拆到 payment-service |
| marketing-service(公用) | 优惠券 CRUD(模块06) | /api/v1/app/marketing/* | **公用**:Banner/领券/我的券,本期骨架列入模块06 一并实施 |
| merchant-service / finance-service | 管理端专属 | 无 | 移动端不直接访问,店铺信息经 goods-service 冗余输出 |

## 鉴权与隔离约定

- C端登录态:JWT(HS256,与后台同密钥),claims 含 `aud=app`、`user_id`、`site_id`;
  `UserAuthMiddleware` 校验 `aud`,拒绝后台 Token 混用,注入 `UserContext`
- 站点隔离:请求头 `X-Site-Id`(游客可用),登录后以 Token 内 site_id 优先;所有查询强制 site_id 过滤
- 网关层客户端签名(X-Client-Id/X-Sign,ClientSignMiddleware)在模块08 部署时启用,开发期可关
- 响应格式复用 `Result::success/page/error`,错误码复用 `ErrorCode`

## 任务清单

### shared 扩展
- [x] UserContext(协程级 C端用户上下文)
- [x] UserAuthMiddleware(Bearer JWT,aud=app 校验)

### user-service(/api/v1/app)
- [x] 服务骨架(composer/Dockerfile/config/bin)
- [x] auth:register、login(密码)、logout、refresh
- [x] user:me、update(昵称/头像)、change-password
- [x] user:balance-logs、points-logs、feedback 提交/列表
- [x] 手机号/邮箱 AES 加密存储 + 脱敏返回;登录写 user_action_log

### goods-service(/api/v1/app/goods)
- [x] 服务骨架
- [x] home:推荐/热门商品聚合(is_recommend/is_hot)
- [x] category/list:分类树、商品分页搜索(关键词/分类/类型/星级/排序)
- [x] detail:商品详情 + 房型/票种 SKU + 退改规则
- [x] calendar:SKU 价格库存日历(日期区间,无日历记录回退 base_price/base_stock)

### order-service(/api/v1/app/order)
- [x] 服务骨架
- [x] create:下单(SKU 校验、日历价计算、事务行锁锁库存、订单号生成)
- [x] pay(mock):模拟支付成功→已支付,locked→sold,生成核销码;正式渠道模块06 payment-service 接管
- [x] list/detail:我的订单分页(状态筛选)、详情(已支付含核销码,联系人脱敏)
- [x] cancel:待支付取消并释放库存
- [x] refund:申请退款→待商户审核单(审核流归管理端模块06);退改规则算可退金额由审核侧实施
- [x] verify-code:核销码查询(二维码内容)

### system-service 补充
- [x] /api/v1/app/site/list:启用站点列表(公开)
- [x] /api/v1/app/site/config:站点货币/语言/时区 + app 组差异化配置(公开)

### 验收
- [x] 全部新增 PHP 文件 php -l 通过(全量 116 文件 0 错误)
- [-] Docker 运行联调(归模块08)

## 完成记录

- 2026-07-28 全部任务完成:
  - shared 新增 UserContext/UserAuthMiddleware;user-service(9502)、goods-service(9503)、order-service(9504)三服务落地
  - 库存采用 goods_daily_stock 行锁(lockForUpdate)+库存流水,无日历记录按基础价/库存补建
  - 支付本期为 order-service 内 mock(pay_trade_no 前缀 MOCK),退款仅提交申请单,审核/回补库存归模块06
  - 增量 SQL:database/user/02-user-app.sql(mobile_hash 检索列)
- 2026-09-07(C-M6,9月计划第1周):marketing-service 的 C 端优惠券链路补齐。
  - **统一券口径**:新增 `app/Service/CouponView.php` 作为优惠券字段的唯一出口 ——
    领券中心 / 活动详情 / 券详情 / 我的券 / 结账择优共用同一套
    「券类型 / 优惠值 / 门槛 / 封顶 / 适用酒店房型 / 有效期 / 叠加规则 / 状态 / 不可用原因」;
    状态(`claimable|unclaimable|available|unusable|used|expired|void`)与
    不可用原因(`sold_out|limit_reached|not_started|expired|used|void|min_amount|scope|offline`)
    **一律下发机器码**,文案由各端 i18n 决定,后端不给 App 下发中文。
    「能不能领」与「列表上写着能不能领」共用 `CouponView::template()` 的判定,不会出现两套结论。
  - **新增两个路由**(`/api/v1/app/marketing/`):`GET /coupon/detail`(`receiveId` 我的券 /
    `couponId` 券模板,同一套字段)与 `POST /coupon/redeem`(促销码兑换)。
  - **促销码兑换**校验站点、促销码状态、有效期、总量、每人限兑、重复兑换,再校验目标券模板本身
    是否可领,成功后在同一事务内写 `marketing_coupon_receive` + `marketing_promo_code_redeem`
    并累加两处计数。失败按五类拆成独立错误码(shared `ErrorCode` 新增
    `PROMO_CODE_NOT_FOUND=40411` / `PROMO_CODE_EXPIRED=40911` / `PROMO_CODE_EXHAUSTED=40912` /
    `PROMO_CODE_DUPLICATED=40913` / `PROMO_CODE_INELIGIBLE=40914`)——
    只用 40401 + 40901 两个码,App 分不出「过期」和「兑完」。
  - **`bestMatch` 增加 `skuId` 入参**并改走 `CouponView::receive($row, $ctx)`,
    抵扣计算与不可用判定与详情页同源(结账接入见 9 月计划第 2 周)。
  - 迁移 `database/marketing/08-consumer-coupon-promo.sql`(幂等,compose initdb 登记为 `99m-`):
    `marketing_coupon` 补 `sku_ids`(适用房型)与 `stackable`(叠加规则);
    `marketing_promo_code` 补 `coupon_id`(兑换发放的券模板,0 = C 端不可兑换);
    新增 `marketing_promo_code_redeem` —— 原表只有 `usage_count` 总量、没有按人的记录,
    `per_user_limit` 实际上一直无法生效。
  - 促销码的每人限兑:后台未配置(`per_user_limit<=0`)时**按 1 次处理**,
    因为「一个码可无限次兑同一张券」不是合理默认。
  - 完整记录与实测结果见 HANDOFF「★ 2026-09-07(C-M6/C-M6.1 优惠中心真实化)」。
- 2026-09-07(C-M6,9月计划第2周):marketing-service 新增结账选券接口
  `GET /api/v1/app/marketing/coupon/match-list` —— 给定 `orderType/goodsId/skuId/amount`,
  返回本人全部未使用券的 `{ list, best }`,每张券带**本单实际抵扣额**与不可用原因,
  可用的按抵扣额从大到小排前面。整段复用 `CouponView::receive($row, $ctx)`,不另写一套判定。
  与 `bestMatch` 分工:后者只回一张(进结账自动应用),前者回整张列表(手动更换时要能解释每一张为什么不能选)。
  抵扣公式与 order-service `PricingService::resolveCoupon` 逐行一致,已用「match-list 给 5000 →
  order/create 实扣 5000」端到端核对过。
  **已知不同源**:`resolveCoupon` 不校验 `sku_ids`(适用房型)而 `CouponView` 校验,
  方向安全(客户端更严)但需在第2周「创建订单再次校验资格」那一行补齐。
