# 模块08:部署与网关(deploy)

状态:部分完成(08-6 启动验证已通过,08-7 全链路联调待执行) | 进度:84% | 依赖:模块01~07 | 更新时间:2026-09-12

## 目标

提供本地一键启动的容器化环境(Windows 本机无 Swoole,后端统一 Docker 运行),OpenResty 网关统一路由分发,支撑前后端联调。

## 任务清单

- [x] 08-2 权限键前后端统一:后端 #[Permission] 注解键全量改成菜单种子 perm_key(shared 注解/切面支持多键任一匹配)
- [x] deploy/docker-compose.yml:MySQL 8.0(挂载 database/ 自动初始化)、Redis 7、8 个 Hyperf 服务、OpenResty 网关
- [x] Hyperf 通用 Dockerfile(各服务已自带 hyperf/hyperf:8.1-alpine-v3.18-swoole 镜像 Dockerfile,compose 直接引用;dev 用 SCAN_CACHEABLE=false 环境变量覆盖)
- [x] OpenResty 网关配置:
  - `/api/v1/admin/auth|sys|site|client → system-service`(实际路由二级模块,与原计划 sys|site|client 略有出入,以 routes.php 实现为准)
  - `/api/v1/admin/merchant|supplier → merchant-service`
  - `/api/v1/admin/goods → goods-service`
  - `/api/v1/admin/order → order-service`
  - `/api/v1/admin/finance → finance-service`
  - `/api/v1/admin/user → user-service`
  - `/api/v1/admin/marketing → marketing-service`
  - `/api/v1/app/site → system-service`;`/api/v1/app/auth|user → user-service`;`/api/v1/app/goods|order → 对应服务`
  - `/api/v1/payment → payment-service`(回调 /api/v1/payment/callback/stripe|paypal)
  - CORS、限流(limit_req 30r/s burst60)、统一错误 JSON(404/429/50x → {code,message,data})
- [x] .env 样例文件(数据库/Redis/JWT密钥/AES密钥)→ deploy/.env.example
- [x] admin-web vite proxy 指向网关(127.0.0.1:8080;client-app 默认已是 localhost:8080 无需改)
- [x] 08-6 启动验证:docker compose 全容器 Up + 八服务 healthz + 网关链路 401 + 环境变量注入(2026-07-30 四步验证全部通过,实际输出见完成记录)
- [~] 08-7 联调验证:登录 → 动态菜单 → 管理员 CRUD → 商户审核 → 商品上架 → 订单查询 全链路走通(Docker 已就绪,待执行)
- [~] 移动端冒烟联调(client-app → /api/v1/app/*)(归 08-7,待执行)
- [x] shared 包单测(模块01 遗留)→ backend/shared/tests/ 26 用例全过,顺带修复 2 个真实 bug
- [x] deploy/k8s/ 目录预留(本期不实现,已置 README 说明)
- [x] `scripts/auto-deploy.sh` 支持指定 target 强制发布:跳过 fetch/merge/变更判断,直接发布前端或重启指定服务/网关
- [x] 生产 MySQL 版本迁移:全局版本命名、执行账本、checksum 防改写、并发锁、失败阻断与空库初始化衔接

## 验收标准

- `./mtrip.sh build` 后,admin-web 登录并完成核心操作链路
- 网关路由、CORS、错误码格式与文档规范一致

## 完成记录

- 2026-09-12 空库增量迁移修复：本地首次启动后 `schema_migrations` 为空，`merchant_kyc_template` 只有 9 条旧业态模板，后台仅查询 `unified` 因而无数据。MySQL 日志显示挂载的 `99z-run-migrations.sh` 报 `bad interpreter: Permission denied`，旧健康检查只排除 `running/failed`，空账本被误判健康。已用容器内 runner 补跑 3 个迁移并登记 `applied`；改用 `deploy/mysql/Dockerfile` 将 runner 以 0644 放入镜像供 entrypoint source，健康检查要求已应用数等于迁移文件数且无未完成记录。`scripts/db-migrate.sh` 与 init runner 同步兼容 macOS 自带 Bash 3.2，迁移状态机测试通过。隔离全新数据卷实测 3 个版本自动执行、统一 KYC 模板 1 条且 `docs` 为 6 项，随后清理隔离卷；原有数据卷保留。当前网关加密登录及 `kyc-templates` 接口均返回 HTTP 200 / `code=0`，模板列表有 1 条。

- 2026-09-09 生产 MySQL 增量迁移接入自动发布:
  - 现状缺口确认:`auto-deploy.sh` 原先只收集本次 commit 改动的 SQL，默认告警；`--apply-db` 调 `db-apply.sh` 重灌文件但没有执行记录，无法发现生产库历史漏迁移或脚本被改写。
  - 新增 `mtrip_system.schema_migrations` 账本与 `scripts/db-migrate.sh`。脚本扫描完整迁移目录、校验全局版本/路径/SHA-256，以 MySQL `GET_LOCK` 串行执行，只对账本缺失版本前进；每次执行带唯一 `attempt_id`，并发进程只能更新自己创建的 running 行；并发成功判定同时核对版本、路径和 checksum，每批结束再重新读取完整账本收口；记录 Git commit、执行节点、开始/结束、耗时、状态，失败立即停止后续版本。
  - 新约定:生产增量只放 `database/migrations/VYYYYMMDDHHMMSS__lower-kebab.sql`（有效 UTC 时间、全局唯一、只增不改）；旧业务目录保留为空库快照。新业务 SQL 若仍加到旧目录，或把旧快照 rename/copy 为版本迁移，自动发布直接拒绝。
  - `auto-deploy.sh --prod` 默认在任何前后端发布前执行完整账本对比；数据库失败会阻断代码发布。`.git/mtrip-last-successful-deploy` 只在整批迁移/构建/重启全成功后推进，故 Git 已快进但发布失败时，下次 cron 仍按旧成功点重放同批代码动作。保留 `--apply-db` 给非生产显式执行，新增 `--skip-db` 供外部 DBA 已完成迁移的应急场景，以及 `database|db|mysql` 强制目标。
  - compose 首次建库先建账本、最后遍历整个迁移目录并登记；init runner 独立调用 mysql 客户端，不依赖官方 entrypoint 只在 source 模式可见的内部函数，失败会把 attempt 记为 `failed`。MySQL 健康检查同时要求账本不存在 `running/failed`，防止部分 DDL 已提交的初始化卷被判为可用。因此以后新增版本不再逐条追加 volume 映射。历史 `db-apply` 明确降级为仅开发环境补灌工具。
  - 验证:`bash -n` 三个生产 shell 脚本与测试脚本通过；`db-migrate.sh --validate` 通过（当前 0 个业务版本）；无 Docker/MySQL 的 fake driver 状态机测试覆盖非法日期/命名、首次建账本、执行、迁移后全账本复核、重复运行跳过、failed/删除历史/倒序版本/checksum 阻断、并发 attempt 所有权、旧快照 rename 拒绝、auto-deploy dry-run 衔接，以及“首次无成功标记时 Git 已快进但迁移失败，下一轮仍重放网关动作并推进成功标记”。本机无 Docker CLI，compose config、健康检查命令与真实 MySQL 仍待 Docker 环境复验。统一 `scripts/check.ps1` 已尝试，但宿主机命中 PHP 7.2，无法解析项目 PHP 8.1 语法并在第 1 步停止；本次未改 PHP。
- 2026-09-02 `scripts/auto-deploy.sh` 新增强制发布 target 模式:
  - 带非选项目标即进入强制发布,跳过 `git fetch`、远端落后判断、`ff-only merge` 与工作区干净门禁;默认无目标的 cron 模式仍保持原安全策略。
  - 支持 `admin-web|merchant-web|supplier-web` 直接 `npm run build` 并原子发布到 `deploy/web/<name>/`;支持后端 `*-service` 重启主池、`*-service-app` 重启 APP 孪生、`gateway|openresty` 重启网关。
  - `client-app|mobile|app` 明确只提示单独 Expo/商店发版,避免误报已部署。
  - 验证:`bash -n scripts/auto-deploy.sh`、`scripts/auto-deploy.sh --dry-run admin-web`、`bash scripts/auto-deploy.sh --dry-run goods-service goods-service-app gateway` 通过;脚本已补执行位。
- 2026-07-28 08-2 权限键前后端统一完成:
  - shared 升级:`Permission` 注解支持 `string|array` 多键 + `keys()` 归一化;`PermissionAspect` 改用 `AdminContext::hasAnyPermission()`(多键任一匹配,超管直接放行)。
  - 业务五服务 53 处注解键全量改为菜单种子 perm_key(system-service 原本就一致未动):merchant/supplier → `merchant:list:*`/`supplier:list:*` 系;goods 酒店门票共用接口用双键 `['goods:hotel:x','goods:ticket:x']`;供应商结算接口被 supplier/settle 与 finance/ssettle 两页共用 → 双键;order 改价/取消 → `order:all:cancel`、备注 → `order:all:list`(前端无按钮防护,降为页面级);退款审核/到账确认 → `order:refund:audit`;提现审核/打款复用 `finance:msettle:confirm|pay`(与前端 v-perm 一致);coupon 发布 → `:edit`、停发/结束 → `:stop`。
  - 验收:83 个注解键全部存在于 `database/seed/02-menu.sql`;18 个改动文件 php -l 零错误。
- 2026-07-28 deploy/ 基础设施落地:
  - `deploy/docker-compose.yml`:MySQL 8.0(database/ 十八个 SQL 按 00→10→20...→90 编号挂载 docker-entrypoint-initdb.d 自动初始化,宿主机 3307)+ Redis 7(6380)+ 八服务(9501~9508,复用各自 Dockerfile,构建上下文 ../backend)+ OpenResty 网关(8080);YAML 锚点 x-hyperf-common 统一环境变量,dev 覆盖 SCAN_CACHEABLE=false。
  - `deploy/openresty/nginx.conf + conf.d/mtrip.conf`:map 路由表按二级模块分发(admin: auth|sys|site|client→9501、merchant|supplier→9505、goods→9503、order→9504、finance→9506、user→9502、marketing→9507;app: site→9501、auth|user→9502、goods→9503、order→9504;/api/v1/payment→9508);CORS 头+OPTIONS 直接 204;limit_req 30r/s burst 60;404/429/50x 统一 {code,message,data} JSON。
  - `deploy/.env.example`(MySQL/Redis/网关端口/JWT/AES)、`deploy/k8s/README.md` 预留;admin-web vite proxy 改指 127.0.0.1:8080。
- 2026-07-28 08-8 shared 包单测完成(本机 PHP 8.1 直跑,无需 Docker):
  - 新建 `backend/shared/tests/`:bootstrap.php(PSR-4 自加载 + Hyperf AbstractAnnotation/Context 桩,无 vendor 可独立运行)+ cases/SupportTest.php + cases/PermissionTest.php + run.php 入口;运行 `D:\BtSoft\php\81\php.exe backend/shared/tests/run.php`,26 用例/96 断言全部通过。
  - 覆盖:Result/ErrorCode/BusinessException/MaskHelper(5 种脱敏+递归参数)/CryptoHelper(往返、随机IV、错误密钥)/JwtHelper(签发校验、篡改、40101/40102)/OrderNoGenerator(格式+唯一性)/Permission::keys 多键归一化/AdminContext(hasPermission/hasAnyPermission/超管/scopeSiteId 站点隔离)。
  - 单测抓出并修复 2 个真实 bug:①CryptoHelper::decrypt 最短长度误写 29 导致空串明文(28 字节密文)解密必报错 → 改 28;②OrderNoGenerator::orderNo 第4段纯随机同毫秒碰撞(1000 次重复 35)→ 改随机起点自增序列 mod 10000(格式不变仍 22 位)。php -l 零错误。
  - ClientSignMiddleware 中间件本体依赖 DB/Redis,归 08-7 Docker 联调验证(签名涉及的 CryptoHelper 密钥解密已覆盖);模块01 据此升 100%。
- 2026-07-28 **08-6 启动验证阻塞**:本机无 docker 命令,且 WSL2 虚拟机平台服务不可用(HCS_E_SERVICE_NOT_AVAILABLE);需安装/启用 Docker Desktop(WSL2 后端)后执行 `cd deploy; docker compose up -d --build`,08-7 联调同样依赖。
- 2026-07-28 08-9 收尾:经用户确认,08-6 启动验证与 08-7 联调延后至 Docker 环境就绪,本模块定格 60%;`docs/guides/setup/启动开发指南.md` 已重写(新增 Docker Compose 一键启动节、补齐 9506~9508 端口表、代理改指网关、WSL2 故障处理 FAQ)。
- 2026-07-28 新增 `deploy/docker-compose.override.yml` 开发期热更新配置:本地 app/、config/、shared/src/ 挂载进容器覆盖镜像内代码(shared 经 composer path symlink 穿透),改代码后 `docker compose restart xxx-service` 约 2 秒生效,免重建镜像;各服务 runtime/logs 挂出到 `deploy/logs/<服务名>/`(按天滚动 14 份,已 gitignore),宿主机直接查日志改配置;生产用 `-f docker-compose.yml` 显式指定跳过合并;启动指南新增 2.2 节「改代码后如何生效」。
- 2026-07-28 启动指南新增 2.0 节「Windows 安装 Docker Desktop 与配置 WSL2」:四步流程(BIOS 虚拟化确认 → 启用虚拟机平台+WSL 功能(无需 Hyper-V)→ 重启 → Docker Desktop WSL2 后端)+ 诊断命令表;实机确认本机 BIOS 虚拟化已开、仅缺「虚拟机平台」可选功能(HCS_E_SERVICE_NOT_AVAILABLE 根因)。
- 2026-07-30 **08-6 部署后四步验证完成**(按 deploy/README.md 第 4 节,Docker 29.6.2 / Compose v5.3.1,实际输出如下):
  1. **容器状态**:`docker compose ps` 共 11 个容器全部 Up——8 个服务(9501~9508)+ 网关 mtrip-gateway-1(8081→80)+ mysql(3307,healthy)+ redis(6380,healthy)。
  2. **探活 9501~9508**:`curl.exe -s http://localhost:950X/healthz` 八服务全部返回 `{"status":"ok","service":"xxx-service"}`(system/user/goods/order/merchant/finance/marketing/payment 名称逐一对应,无一超时)。
  3. **网关链路**:`curl.exe -s -o NUL -w "%{http_code}" http://localhost:8081/api/v1/app/auth/login -X POST` 返回 **HTTP 401**,符合预期(无签名头被 ClientSignMiddleware 拦截,链路与签名中间件正常;未出现 502,无需按第 3 节重启网关)。
  4. **环境变量注入**:`docker exec mtrip-user-service-1 sh -c "env | grep MTRIP_SUBMIT"` 输出 `MTRIP_SUBMIT_LOCK=true`、`MTRIP_SUBMIT_LOCK_TTL=10`,.env 注入生效。
  - 结论:恢复清单第 1~3 步验证通过,模块进度 60% → 80%;剩余 08-7 全链路联调(下方清单第 4~6 步)完成后升 100%。注:网关宿主机端口现为 **8081**(compose 实际映射 8081→80,旧文档中的 8080 以此为准修正)。

## ★ 2026-09-03:启动方式修正(网关无限重启)

自 commit `0921843`(管理端 / APP 端服务分离)起,网关 conf 有 5 条 upstream 指向 **APP 孪生池**
(`*-service-app`,定义在 `docker-compose.app-pool.yml`)。**裸 `docker compose up -d` 起不出孪生**,
nginx 启动期解析不到主机名会 `[emerg] host not found in upstream` 退出并被反复拉起 —— 表现为
`mtrip-gateway-1` 一直重启。**唯一正确入口是 `deploy/mtrip.sh`**(dev 模式固定合并 4 个 compose 文件)。
`CLAUDE.md` / `deploy/README.md` / `docs/guides/setup/启动开发指南.md` 的启动命令已同步改写。
另修 `mtrip.sh health` 的网关探针误报(`MTRIP_CLIENT_SIGN=false` 时应得 400 而非 401)。
完整记录见 HANDOFF「★ 2026-09-03(网关无限重启的真因)」。

## 恢复联调清单(Docker 就绪后按此执行)

1. ~~`cd deploy; cp .env.example .env; docker compose up -d --build`~~ ✅ 2026-07-30 已完成(11 容器全部 Up)。
2. ~~逐容器看健康:`docker compose ps`;探活 9501~9508 healthz~~ ✅ 2026-07-30 八服务 healthz 全部 ok(实际输出见完成记录 08-6)。
3. ~~网关链路:无签名头 POST http://localhost:8081/api/v1/app/auth/login~~ ✅ 2026-07-30 返回 401 符合预期(签名中间件在网关链路上生效)。
4. admin-web `npm run dev` → 登录(种子账号见 database/seed/01-admin-role.sql)→ 动态菜单 → 各业务页 CRUD → 统计大屏。
5. client-app `npm start` 冒烟 /api/v1/app/*(站点/商品/登录)。
6. 验证 ClientSignMiddleware 签名链路与限流/错误 JSON 格式;修复联调发现的问题后,本模块升 100%。
