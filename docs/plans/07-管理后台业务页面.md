# 模块07:管理后台业务页面(一级菜单 4~12)

状态:已完成(接口联调归模块08) | 进度:100% | 依赖:模块04、模块06 | 更新时间:2026-07-28

## 目标

实现商户、供应商、用户、商品、订单、财务、营销、统计、核销 9 大业务菜单页面;首期重点商户/商品/订单三大核心 + 数据大屏,其余占位页 + 列表骨架。

## 任务清单

### 首期完整实现
- [x] 数据大屏首页:KPI 4 卡片(总营收/今日订单/待结算/平台佣金,环比箭头+迷你折线)、营收趋势折线、订单量柱状(酒店/门票双系列)、站点/商户排行、实时订单滚动;暗色适配
- [x] 商户管理:商户列表(状态彩色标签、待审核角标)、商户详情抽屉(基础/资质图片预览/结算账户脱敏/统计)、审核页(通过绿/驳回橙双按钮+意见)、费率配置、启停/注销高危确认
- [x] 商品管理:商品列表(酒店/门票 Tab)、新增/编辑分步表单(基础信息→图文素材→价格库存→退改规则→提交审核)、房型分时库存日历组件(日期×房型矩阵,低库存标红,批量框选改价调库存)、商品审核工作台、分类管理
- [x] 订单管理:全部订单列表(多维筛选、状态进度条)、订单详情抽屉(支付/退款/核销/日志)、退款审核页(可退金额高亮+退改规则悬浮)、手动核销/撤销弹窗、导出

### 首期骨架(列表页可查,操作后续补)
- [x] 供应商管理 4 页
- [x] C端用户管理 4 页
- [x] 财务管理:资金总览卡片(三色分账)、资金流水列表、商户结算/提现审核、税费配置(税费配置无后端接口回退 wip)
- [x] 营销管理:优惠券卡片列表、限时活动、Banner 配置(左表单右预览)、积分规则(活动/Banner/积分无后端接口回退 wip)
- [x] 数据统计:站点/商户/商品/财务四维报表页
- [x] 核销管理:核销日志页(设备状态/核销规则无后端接口回退 wip)

## 通用要求

- 三大页面模板复用:数据看板页/列表表格页/表单详情页
- 金额分级色、状态彩色标签、敏感信息脱敏、无权限按钮隐藏
- 待办角标:待审核商户、待退款订单数量联动侧边菜单

## 完成记录

- 2026-08-25 待核实、重新提交、得到正式认可和已拒绝四个共用表格的线索 ID 改为 `application_no`，展示和导出均使用入驻申请阶段的 `APP-XXXX`，移除 `#数据库主键` 回退显示。
- 2026-08-25 入驻申请和商户验证详情中的“商户 ID”统一展示 `MCH-XXXX` 业务编号，不再展示或依赖批准后才存在的数据库数字主键；入驻批准成功提示同步显示该业务编号。
- 2026-08-25 入驻线索术语与列表调整：录入弹窗改为“商户入驻线索”，表单统一为商户名称、业务类型、业务数量、注册业务和业务名称；列表列名改为商户名称、业务名称、提交日期，业务名称展示全部注册业务、商户名称副标题固定展示注册国家/地区、日期仅保留 `YYYY-MM-DD`，操作列仅保留详情图标。
- 2026-08-25 入驻申请“录入入驻线索”表单按录入范围收敛：公司区仅展示公司名称、公司/集团名称、公司注册号、注册国家/地区、企业类型、企业数量；注册商家区默认一条，展示商家名称、类型、城市、业务联系人、手机号码、电子邮箱。保存前校验公司名称和至少一家注册商家，移除公司层商家名称、城市及注册地址。
- 2026-08-25 重新提交详情“拒绝申请”功能完成：拒绝理由 options 修正为 value/label，9 项原因可正确选择，弹窗文案统一为拒绝申请；成功后关闭抽屉并自动跳转已拒绝页面，状态导航按路由重新挂载以即时刷新数量。vue-tsc、生产构建及本地浏览器弹窗验证通过，验证过程未提交真实拒绝数据。
- 2026-08-25 重新提交详情底部按钮尺寸回归 Ant Design Vue 默认规格：保留三种原型配色和对应图标，移除固定宽高、字号、内边距与圆角覆盖，实际为 32px 高、14px 字号、4px 圆角、`4px 15px` 内边距和 8px 组间距；局部样式不影响待核实操作栏。vue-tsc、生产构建及本地浏览器实测通过。
- 2026-08-25 重新提交详情操作栏整改：专用抽屉由 1060px 收窄为 760px，底部固定展示“请求重新提交 / 拒绝申请 / 批准商户”；请求操作复用必填补正说明与通知弹窗，拒绝操作复用预置理由与补充说明弹窗，批准操作完整复用待核实页批准凭证流程和审批门禁。vue-tsc、生产构建及本地浏览器宽度/定位/弹窗视觉冒烟通过。
- 2026-08-24 待核实“批准商户”按原型改造：按钮名称统一为批准商户；弹窗增加验证完成提示、商户访问码、仅显示一次的初始密码、邮件/短信/应用内渠道与登录通知预览；批准后父级详情保持打开，并在底部展示访问权限、复制/重新生成及交付信息。admin-web/merchant-web 构建和本地浏览器视觉冒烟通过。
- 2026-08-24 重新提交详情侧边栏按原型完成：重新提交队列使用 1060px 专用布局，包含请求摘要、公司信息、注册商家切换和文件对照卡；卡片左侧展示被拒绝原稿、拒绝理由与审核信息，右侧展示新稿、审核操作或等待商户提交状态，确认按钮仅在对应新稿齐备后启用。vue-tsc、生产构建和实际数据浏览器视觉冒烟通过。
- 2026-08-24 onboarding“提交核验”由应用级 confirm 改为业务单元级 submit-verification，携带当前 businessId；上传成功不再前端伪造待核验状态，资料变更同步显示待办中，提交成功刷新详情与列表。vue-tsc 零报错。
- 2026-07-28 【07-1 商户管理】完成并通过 npm run build 零 TS 报错:
  - `src/api/merchant.ts`:商户 11 接口 + 供应商 14 接口(供 07-4 复用)
  - `views/merchant/list/index.vue`(590行):筛选/详情抽屉(资质图预览+账户/子账号表)/新增编辑(密文留空保留)/审核 Modal(通过展示一次性账密)/费率 Modal/启停 popconfirm/注销高危 Modal(必填备注)
  - `views/merchant/account/index.vue`:远程搜索选商户 + 结算账户维护(账号加密,编辑留空保留)
  - `views/merchant/stats/index.vue`:统计 4 卡片 + 结算对账单表格 + CSV 导出
  - merchant/perm 无后端接口 → dynamic.ts 自动回退 wip,不建文件
- 2026-07-28 【07-2 商品管理】完成并通过 npm run build 零 TS 报错:
  - `src/api/goods.ts`:商品/分类/房型/票种/退改规则/库存日历全部 26 接口
  - `views/goods/components/GoodsManage.vue`(共享组件):酒店/门票复用——筛选(分类级联/商户远程搜索)/详情抽屉(图集预览+SKU+退改规则)/新增编辑(草稿)/提审/上下架/软删/SKU 管理抽屉(房型床型早餐·票种分时时段)/退改规则抽屉(免费退/阶梯退动态行/不可退)
  - `views/goods/hotel/index.vue`、`views/goods/ticket/index.vue`:薄壳页(goodsType 1/2)
  - `views/goods/category/index.vue`:两级分类树(酒店/门票 Tab)+增子类/编辑/删除
  - `views/goods/stock/index.vue`:四 Tab——价格库存日历(低库存标红/关房置灰)+区间批量设置(星期过滤)+单日调整/库存总览/低库存预警/变动流水
  - `views/goods/audit/index.vue`:待审核队列(Badge 角标)+审核详情抽屉+通过/驳回 Modal+已上架强制下架(必填原因)
- 2026-07-28 【07-3 订单管理】完成并通过 npm run build 零 TS 报错(10.14s):
  - `src/api/order.ts`:订单 6 + 退款 4 + 核销 3 全部 13 接口
  - `views/order/components/OrderManage.vue`(共享组件):全部/酒店/门票复用(orderType 0/1/2)——筛选(订单号/状态/商户远程搜索/联系人/下单区间/使用日期)/8 状态彩色标签/详情抽屉(订单描述+退款单子表+核销日志子表)/待支付改价 Modal(≤总额+必填原因)/取消 Modal(必填原因,danger)/备注 Modal/CSV 导出(perm order:all:export,当前筛选前 2000 条)
  - `views/order/all|hotel|ticket/index.vue`:薄壳页
  - `views/order/refund/index.vue`:退款列表+详情抽屉(退款单+凭证图预览+订单快照)+审核 Modal(商户/平台双环节提示,平台通过可核定金额≤申请额,驳回必填意见)+到账确认 Modal(流水号选填)
  - `views/order/verify/index.vue`:手工核销卡片(按核销码)+核销日志(方式/状态筛选)+撤销核销高危 Modal(必填审批备注,perm order:verify:revoke)
  - `components/StatusTag.vue`:StatusItem.color 联合类型扩展 cyan/orange/purple(a-tag 原生预设色)
  - order/export 导出对账页无后端接口 → dynamic.ts 自动回退 wip,导出功能由订单页 CSV 按钮承担
- 2026-07-28 【07-4 供应商+用户】完成并通过 npm run build 零 TS 报错(10.04s):
  - `src/api/user.ts`:C端用户 3 + 反馈 2 全部 5 接口(供应商接口沿用 merchant.ts)
  - `views/supplier/list/index.vue`(513行):筛选(名称/类型/结算方式/状态)/详情抽屉(合作信息+结算账户脱敏)/新增编辑双列表单(creditCode 新增必填编辑禁改,contactPhone/accountNo 密文留空保留)/审核 Modal(通过=已合作记合作起始,驳回必填意见)/暂停恢复 popconfirm(暂停联动停供)/终止高危 Modal(必填备注,须无未回款账单,danger)
  - `views/supplier/goods/index.vue`:顶层供应商远程搜索(带状态)→ 供货商品列表;仅已合作可新增(supply_price≤retail_price 校验);停供/恢复;sync_type 三态标签;未选供应商时 a-empty
  - `views/supplier/settle/index.vue`:结算单列表(账期月份选择器 YYYY-MM)/确认对账 Modal(通过=待回款,驳回必填意见,perm supplier:settle:confirm)/标记打款 Modal(payVoucher 选填,perm supplier:settle:pay)
  - `views/user/list/index.vue`:筛选(昵称/状态/实名/注册来源/注册日期区间)/列表脱敏手机号/详情抽屉(超管明文手机号+余额流水 amount/积分流水 points 各近10条,列名已对齐 DDL)/冻结解冻 Modal(必填原因留痕,冻结 danger)
  - `views/user/feedback/index.vue`:反馈投诉列表(类型/状态/用户ID/提交日期筛选)/详情抽屉(内容+附件图预览+处理回复)/处理 Modal(流转规则 {0:[1,2,3],1:[2,3]} 动态选项,标记已处理必填回复,perm user:feedback:handle)
  - supplier/report、user/level、user/log 无后端接口 → dynamic.ts 自动回退 wip,不建文件
- 2026-07-28 【07-5 财务+营销】完成并通过 npm run build 零 TS 报错(10.35s):
  - `src/api/finance.ts`:资金总览/流水/提现/商户结算全部 13 接口(注释已对齐 DDL flow_type/biz_type)
  - `src/api/marketing.ts`:优惠券模板 8 + 领券记录 1 全部 9 接口;CouponPayload 用 type 别名(interface 无隐式索引签名传 Record<string,unknown> 会 TS2345)
  - `views/finance/overview/index.vue`:今日/本月/累计三卡片(收绿/支红/净额)+待办卡片(待审核提现/待确认结算)+刷新
  - `views/finance/msettle/index.vue`(594行):商户结算+提现审核双 Tab(提现无独立菜单整合于此)——结算 0确认/标记争议→1标记打款(凭证选填)→2已打款,3解除争议;提现 0审核 Modal→1打款结果(成功流水号选填/失败必填原因);详情抽屉(提现 detail 解密收款账户);权限 finance:msettle:confirm/pay
  - `views/supplier/components/SettleManage.vue`(共享组件,permPrefix prop):原 supplier/settle 全量迁入;`views/supplier/settle` 与 `views/finance/ssettle` 均改薄壳复用(权限前缀 supplier:settle / finance:ssettle)
  - `views/finance/flow/index.vue`:流水筛选(流水号/类型/业务类型/状态/商户远程搜索/日期区间)+金额红支绿收+手动调账 Modal(金额>0+必填原因+超管选站点,perm finance:flow:adjust)+CSV 导出当前筛选前 2000 条(perm finance:flow:export)
  - `views/marketing/coupon/index.vue`(568行):优惠券模板+领券记录双 Tab——状态机 0未开始→发布→1进行中⇄停发2/结束3;新增编辑 Modal(券类型联动字段/指定商品远程搜索/固定日期或领后N天);进行中仅可调总量(≥已领取或0不限);删除仅 0/3;领券记录筛选券模板/用户/状态
  - finance/tax、marketing/activity|banner|points 无后端接口 → dynamic.ts 自动回退 wip,不建文件
- 2026-07-28 【07-6 数据大屏+统计/核销】完成并通过 npm run build 终检零 TS 报错(11.80s):
  - 后端补建 3 个只读统计接口(php -l 零错误):order-service `Admin/AdminStatsController.php`(GET /stats/dashboard 大屏聚合:KPI/日趋势/站点商户排行/最新订单;GET /stats/report 四维报表 dim=site|merchant|goods,退款按 dim_id 二次查询补齐)、finance-service `FinanceController::report()`(GET /finance/report 按年 12 月收支净额+biz_type 拆分)
  - `src/api/stats.ts`:apiStatsDashboard / apiStatsReport / apiFinanceReport 3 接口
  - `components/EChart.vue`:echarts/core 按需注册封装(Line/Bar/Pie,watch option 深监听 setOption notMerge,resize/dispose)
  - `views/stats/dashboard/index.vue`(257行):KPI 4 卡片+营收折线(area)+订单量堆叠柱(酒店/门票)+站点/商户排行(前三金牌)+最新订单;站点名 apiSiteTree 扁平化映射
  - `views/stats/components/StatsReport.vue`(共享组件,props dim/permPrefix/title):三维报表复用——订单数/已支付/销售额/佣金/退款额,CSV 导出;`stats/site|merchant|goods` 三薄壳
  - `views/stats/finance/index.vue`:年度选择+汇总 3 卡+收支双柱净额折线+12 月明细表+CSV(perm stats:finance:export)
  - `views/verify/log/index.vue`:核销日志只读(复用 apiVerifyLogs)+CSV 导出(perm verify:log:export);撤销操作保留在订单菜单核销记录页
  - verify/device、verify/rule 无后端接口 → dynamic.ts 自动回退 wip,不建文件
  - 大屏口径:已支付=order_status IN(1,2,3);待结算=finance_merchant_settle status IN(0,1);财务报表成功流水=flow_status=1
