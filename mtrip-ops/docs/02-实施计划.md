# Mtrip Ops 实施计划

## P0: 独立骨架与设计文档

- [x] 新建 `mtrip-ops/` 独立目录。
- [x] 增加 README、方案设计、实施计划、安全模型、成熟软件调研。
- [x] 增加零依赖 Node 单体服务。
- [x] 增加总览、日志、发布三个页面。
- [x] 增加配置样例与 systemd 样例。

## P1: 只读监控 MVP

- [x] 网关 `/healthz` 探测。
- [x] 主池 8 个服务直连 `/healthz` 探测。
- [x] APP 孪生池占位展示。
- [x] `mtrip.sh status` 输出展示。
- [x] `deploy/logs/*` 日志文件发现与 tail。
- [x] request log 状态码、Top API、慢请求基础统计。
- [ ] 网关 access log 挂载到宿主并纳入统计。
- [x] Docker stats 实时 CPU/内存采集(可用则展示,不可用降级)。
- [x] Docker sudo 诊断接口 `/api/diagnostics/docker` 与服务页诊断卡。

## P2: 发布管理

- [x] 包装 `scripts/auto-deploy.sh --dry-run`。
- [x] 包装 `deploy/mtrip.sh health`。
- [x] 命令执行审计写入 `data/audit.log`。
- [x] 发布前 DB 备份入口(显式开启动作后可执行)。
- [ ] 正式部署动作二次确认。
- [ ] DB apply 文件选择与确认。
- [x] 审计页面:展示命令开始/结束事件。

## P3: 业务监控

- [ ] 配置只读 MySQL 账号。
- [ ] 订单、支付、核销、商户、商品、用户关键指标。
- [ ] 按 `site_id` 筛选。
- [ ] 最近 5/15/60 分钟活跃用户估算。
- [ ] 后续接 Redis TTL 在线用户真实计数。

## P4: 告警与安全

- [ ] 账号登录与 RBAC。
- [ ] 告警规则配置。
- [ ] 邮件/企业微信/Telegram webhook。
- [ ] 危险动作审批或双人确认。
- [ ] docker-socket-proxy 或受限 runner。

## Mtrip 主项目建议修正

- [ ] `scripts/auto-deploy.sh` 的 DB 匹配从 `database/*.sql` 扩展到子目录 SQL。
- [ ] `deploy/mtrip.sh` 统一支持 `DOCKER='sudo docker'` 的 compose 探测与执行。
- [ ] `deploy/openresty` 将 access/error log 挂载到 `deploy/logs/gateway/`。


## P1.5: 完整应用骨架增强

- [x] 新增服务矩阵页 `/services`,合并 health、日志统计和 Docker stats。
- [x] 服务矩阵按异常/风险优先排序:health 失败、APP 路由风险、容器停止/缺失、5xx、慢请求优先。
- [x] 日志中心增加关键词/服务/文件/状态码/慢请求/5xx 搜索。
- [x] 发布工作台增加 DB backup、单服务 logs/restart/build 白名单入口。
- [x] 新增审计页 `/audit`。
- [x] UI 从简单 demo 升级为工作台式布局:服务卡片、过滤栏、发布卡片、状态色。
- [ ] 增加实时刷新/SSE 和折线图。
- [ ] 增加发布审批与二次确认弹层。
- [ ] 增加正式单服务灰度发布流程:预检 → 备份 → build/restart 指定服务 → health → 回滚提示。

## P1.6: APP 池状态与主题模板

- [x] 读取 `docker ps -a`,区分服务容器 `running/stopped/missing`。
- [x] APP 孪生池缺失或停止时明确提示 `/api/v1/app/*` 固定上游可能 502,不会自动回退主池。
- [x] 服务页增加端口、容器状态、Health 耗时、CPU、内存、网络、Block IO、PIDs、请求/慢请求/5xx。
- [x] 增加高密度全量服务表,适合大平台快速扫视。
- [x] 增加玻璃、黑色经典、浅色专业、终端矩阵四套主题模板。
- [x] 增加紧凑/舒展两种显示密度。
- [ ] 将主题选择持久化到用户账号配置(当前为浏览器 localStorage)。
- [ ] 增加大屏模式:自动刷新、全屏、隐藏操作栏。

## P1.7: 主题下拉与发布元数据

- [x] 主题切换从按钮组改成下拉选择。
- [x] 密度切换从按钮组改成下拉选择。
- [x] 服务页支持展示镜像、启动时间、运行时长。
- [x] 服务页支持从 `deploy/release.json`、Docker labels、环境变量读取版本号、发布时间、Git SHA、发布说明。
- [x] Docker 无权限时容器状态显示 `unknown`,不再误判 `missing`。
- [x] 新增 `docs/05-发布版本信息配置.md` 与 `examples/release.example.json`。
- [ ] 在 `scripts/auto-deploy.sh` 发布成功后自动写入 `deploy/release.json`。
- [ ] 在 compose/Dockerfile 增加可选 OCI labels。

## P1.8: Git 发布入口

- [x] 发布页增加 Git 状态区,展示分支、HEAD、dirty 状态、远端摘要和最近提交。
- [x] 增加 `git status -sb`、`git fetch --prune origin`、`git pull --ff-only` 白名单动作。
- [x] 增加发布流程条:Git 拉取 → dry-run → 备份 → 部署/单服务 → health。
- [x] 新增 `docs/06-Git发布流程.md`。
- [ ] 正式 deploy 动作接入二次确认。
- [ ] 发布成功后自动写 `deploy/release.json`。
