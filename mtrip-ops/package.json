{
  "name": "mtrip-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight single-process ops console for Mtrip.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/collectors.js && node --check src/render.js && node --check src/runner.js && node --check src/auth.js"
  },
  "engines": {
    "node": ">=20"
  }
}

